#include "StdAfx.h"
#include "GaitSelector.h"

namespace Test
{
	/*
	 * GaitSelector methods
	 */
	GaitSelector::GaitSelector(Dood* dood) : dood(dood)	{ }

	void GaitSelector::Update(float timestep)			{ }
}
